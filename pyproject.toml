# Config only. Nothing here is installed -- `python tools/run.py` works in a
# clean checkout with nothing but the stdlib, and that property is worth
# keeping at 10:05 tomorrow.

[tool.ruff]
line-length = 100
src = ["src", "tools"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
